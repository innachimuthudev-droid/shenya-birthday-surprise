import {useState} from"react";import {motion} from"framer-motion";import Confetti from"../components/Confetti";import BirthdayScene from"../components/BirthdayScene";export default function CakeCutting({next}:{next:()=>void}){const[cut,setCut]=useState(false);return <section className="card wide"><h1>Cake Cutting Time! 🎂</h1><p>{cut?"The cake is cut! Everyone is celebrating! 🎉":"A simple, normal cake-cutting moment. Make the cut!"}</p><div className="cut-wrap"><BirthdayScene cut={cut}/>{!cut&&<motion.div className="knife" animate={{rotate:[-12,10,-12]}} transition={{duration:1.2,repeat:Infinity}}><i/></motion.div>}</div>{cut?<><Confetti/><h2>Wait... Someone has a message for you. 👀</h2><button onClick={next}>Open Your Message 💌</button></>:<button onClick={()=>setCut(true)}>Cut the Cake! 🔪🎂</button>}</section>}