import {useState} from"react";import Confetti from"../components/Confetti";import {birthday} from"../config";export default function PuzzleChallenge({next}:{next:()=>void}){const[a,setA]=useState("");const ok=a===birthday.idolAnswer;return <section className={"card "+(a&&!ok?"shake":"")}><div className="emoji">🧩</div><h1>One Little Question...</h1><p className="question">Who is your favorite idol? 👀</p><div className="choices"><button className={a==="Vijay"?"picked":""} onClick={()=>setA("Vijay")}>⭐ Vijay</button><button className={a==="Kim Taehyung"?"picked":""} onClick={()=>setA("Kim Taehyung")}>💜 Kim Taehyung</button></div><div className={ok?"success":"error"}>{!a?"Choose one!":ok?"Correct! I knew it! 🎉💜":"Nope! Try again 😄"}</div>{ok&&<><Confetti/><button onClick={next}>Continue →</button></>}</section>}