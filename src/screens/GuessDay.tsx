import {useState} from"react";import Confetti from"../components/Confetti";import {birthday} from"../config";export default function GuessDay({next}:{next:()=>void}){const[d,setD]=useState(""),[ok,setOk]=useState(false),[bad,setBad]=useState(false);return <section className={"card "+(bad?"shake":"")}><div className="emoji">🎯</div><h1>Guess the Day</h1><p>When is Shenya's birthday?</p><input className="date" type="date" value={d} onChange={e=>{setD(e.target.value);setBad(false)}}/><div className="status">{ok?"Correct! 🎉":"Choose a date and check it."}</div>{ok?<><Confetti/><button onClick={next}>Next Challenge →</button></>:<button disabled={!d} onClick={()=>d===birthday.date?setOk(true):setBad(true)}>Check the Date</button>}</section>}