import {useEffect} from"react";export default function Confetti(){useEffect(()=>{const c=document.createElement("canvas");c.className="confetti";document.body.appendChild(c);const x=c.getContext("2d")!,d=devicePixelRatio||1;c.width=innerWidth*d;c.height=innerHeight*d;x.scale(d,d);const b=Array.from({length:90},()=>({x:innerWidth/2,y:innerHeight*.28,vx:(Math.random()-.5)*10,vy:-Math.random()*9-2,s:4+Math.random()*5,a:Math.random()*6}));let id=0;const go=()=>{x.clearRect(0,0,innerWidth,innerHeight);b.forEach(q=>{q.x+=q.vx;q.y+=q.vy;q.vy+=.24;q.a+=.12;x.save();x.translate(q.x,q.y);x.rotate(q.a);x.fillStyle=`hsl(${Math.random()*360},75%,65%)`;x.fillRect(0,0,q.s,q.s*1.8);x.restore()});id=requestAnimationFrame(go)};go();const t=setTimeout(()=>{cancelAnimationFrame(id);c.remove()},2200);return()=>{cancelAnimationFrame(id);clearTimeout(t);c.remove()}},[]);return null}