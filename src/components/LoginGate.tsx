import {FormEvent,useState} from"react";import {birthday} from"../config";export default function LoginGate({onSuccess}:{onSuccess:()=>void}){const[n,setN]=useState(""),[p,setP]=useState(""),[err,setErr]=useState(false);const submit=(e:FormEvent)=>{e.preventDefault();if(n.trim().toLowerCase()===birthday.login.name.toLowerCase()&&p===birthday.login.password){sessionStorage.setItem("shenya-unlocked","1");onSuccess()}else setErr(true)};return <main className="page"><section className="card login"><div className="emoji">🎁</div><h1>A Little Surprise For You</h1><p>Enter your name and password to begin your birthday adventure. ✨</p><form onSubmit={submit}><label>Name<input value={n} onChange={e=>{setN(e.target.value);setErr(false)}} placeholder="Your name"/></label><label>Password<input type="password" value={p} onChange={e=>{setP(e.target.value);setErr(false)}} placeholder="Password"/></label>{err&&<div className="error shake">Wrong details — try again! 💜</div>}<button>Unlock the Surprise 💖</button></form></section></main>}